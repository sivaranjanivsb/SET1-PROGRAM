#include<stdio.h>
void main()
{
int a[n],k,i,sum=0;
printf(" enter the number");
scanf("%d",&n);
for(i=0;i<n;i++)
{
scanf("%d",&a[n]);
}
printf(" enter the limit");
scanf("%d",&k);
for(i=0;i<k;i++)
{
sum=sum+a[i];
}
printf(" the sum of number in an array for a given limit is %d",sum);
}
